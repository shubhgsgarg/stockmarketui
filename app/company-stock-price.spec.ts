import { CompanyStockPrice } from './company-stock-price';

describe('CompanyStockPrice', () => {
  it('should create an instance', () => {
    expect(new CompanyStockPrice()).toBeTruthy();
  });
});
